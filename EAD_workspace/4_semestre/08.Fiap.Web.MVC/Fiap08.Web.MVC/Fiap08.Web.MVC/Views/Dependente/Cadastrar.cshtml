@model Fiap08.Web.MVC.Models.Dependente

@{
    ViewBag.Title = "Cadastrar";
}

<h2>Cadastrar Dependente</h2>

@using (Html.BeginForm())
{

    <div class="form-group">
        @Html.LabelFor(d => d.Nome)
        @Html.TextBoxFor(d => d.Nome, new { @class = "form-control", placeholder = "Digite o nome do dependente" })
    </div>

    <div class="form-group">
        @Html.LabelFor(d => d.DataNascimento)
        @Html.TextBoxFor(d => d.DataNascimento, new { @class = "form-control", placeholder = "dd/mm/yyyy" })
    </div>

    <div class="form-group">
        @Html.LabelFor(d => d.Responsavel)
        @Html.DropDownListFor(d => d.ResponsavelId,
            ViewBag.responsaveis as SelectList,
            "Selecione",
            new {@class = "form-control" })
    </div>


    <input type="submit" value="Cadastrar" class="btn btn-primary" />

}